.DS_Store
.env

# Output of catalog/build_catalog.py. The release holds the published copy.
build/

# The catalog the seed converter matches against. Download it from the
# catalog-latest release; it is 42 MB and rebuilds from the source data.
catalog.sqlite
catalog.sqlite.gz

# Python
__pycache__/
*.pyc

# Xcode. `xcodegen generate` rebuilds the project; derived data never belongs in the repo.
ios/BinderBooks.xcodeproj
ios/CardTracker.xcodeproj
ios/build*
xcuserdata/
*.xcuserstate

# Playwright MCP screenshot output
.playwright-mcp

# Generated by xcodegen from project.yml
ios/Info.plist
