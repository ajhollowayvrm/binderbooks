node_modules
dist
.env
.DS_Store
.sync-token
backups/

# iOS: both are generated. `npm run ios` rebuilds them from project.yml and the
# Vite build, so neither belongs in the repo.
ios/www
ios/BinderBooks.xcodeproj
# glob, not `ios/build`: a device build uses its own derived-data path
# (ios/build-device), and an exact match silently let that whole tree into a commit
ios/build*

# Playwright MCP screenshot output — a tool artifact, not project code
.playwright-mcp
